[package]
name = "quantumvault-jose"
description = "Post-quantum JOSE library — JWT/JWS with ML-DSA-44/65/87 (NIST FIPS 204)"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true

[dependencies]
quantumvault-core = { path = "../quantumvault-core" }

# Serde + JSON for headers/claims
serde.workspace = true
serde_json.workspace = true

# Base64URL (no padding) per RFC 4648 §5
base64.workspace = true

# Time handling for `exp`, `nbf`, `iat`
chrono.workspace = true

# Errors
thiserror.workspace = true

[dev-dependencies]
proptest.workspace = true
